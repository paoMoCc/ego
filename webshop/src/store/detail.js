const state = {
  goodInfo: {},
};
const mutations = {
  GETGOODINFO(state, goodInfo) {
    state.goodInfo = goodInfo;
  },
};
const actions = {
   
};
//简化数据而生
const getters = {
};
export default {
  state,
  actions,
  mutations,
  getters,
};
